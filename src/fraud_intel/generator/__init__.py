"""Deterministic, seeded synthetic generators, one per channel (guide
section 7). No database, Docker, or network access anywhere in this
package -- every generator returns in-memory Pydantic objects only."""
