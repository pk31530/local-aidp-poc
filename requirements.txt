# ---- Core ----
python-dotenv==1.0.1
PyYAML==6.0.2
pydantic==2.9.2
pydantic-settings==2.6.1

# ---- Data processing ----
polars==1.9.0
pyarrow==17.0.0
duckdb==1.1.3
pandas==2.2.3
numpy==1.26.4

# ---- ML ----
# macOS: xgboost needs the OpenMP runtime -> `brew install libomp` (not
# bundled; without it src/ml/train.py falls back to RandomForestClassifier).
xgboost==2.1.2
scikit-learn==1.5.2
mlflow==2.17.2

# ---- Streaming / storage clients ----
# Pinned below 2.4: librdkafka >=2.4's default Fetch API version negotiation
# (v12) isn't supported by Redpanda as of v25.2.1, and neither
# api.version.request=false nor broker.version.fallback suppresses it —
# confirmed via direct testing (see BUILD_LOG.md Phase 7).
confluent-kafka==2.3.0
minio==7.2.10
boto3==1.35.54
psycopg2-binary==2.9.10

# ---- API / dashboard ----
fastapi==0.115.4
uvicorn[standard]==0.32.0
streamlit==1.39.0
httpx==0.27.2

# ---- Synthetic data ----
Faker==30.8.2

# ---- Logging / resilience ----
structlog==24.4.0
tenacity==9.0.0

# ---- Testing ----
pytest==8.3.3
pytest-asyncio==0.24.0
